node_modules/
npm-debug.log*
.DS_Store
*.log
pnpm-lock.yaml

# MCP config (developer-local, not committed)

# reagent — AI tooling (stays on developer machine, not committed)
.claude/agents/
.claude/hooks/
.claude/settings.json
RESTART.md

# Generated during publish (not for commit)
sbom.json
*.tgz

# TypeScript build output
dist/

# Test artifacts
src/__tests__/gateway/_mock-downstream.mjs

# reagent runtime (HALT is committed when active; policy.yaml is committed)
# .reagent/ itself is committed — do not ignore it
.reagent/tasks.jsonl
.reagent/.last-checkin
.reagent/audit/
.reagent/tasks.lock
/.scratch/

# Rust build artifacts (daemon/ crate — kept for historical reference)
daemon/target/
