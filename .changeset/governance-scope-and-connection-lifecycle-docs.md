---
'@bookedsolid/reagent': patch
---

Add Governance Scope guide and Connection Lifecycle reference — explains `.mcp.json` vs `gateway.yaml` decision, what governance actually means for proxied servers, and connection startup/crash/shutdown behavior
