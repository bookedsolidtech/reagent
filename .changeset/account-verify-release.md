---
"@bookedsolid/reagent": patch
---

feat(account): add `account verify [--all]` to confirm token identity against Anthropic API; remove dead OAUTH_REFRESH_TOKEN export; preserve subscriptionType/rateLimitTier in credential reads; add token expiry warnings to account env
