---
'@bookedsolid/reagent': minor
---

Add multi-credential management (`reagent account`) for switching between multiple Claude billing accounts. Store OAuth tokens in macOS Keychain, swap with `rswitch` shell function or `eval "$(reagent account env <name>)"`. Includes 8 CLI subcommands, comprehensive documentation, and security threat model.
